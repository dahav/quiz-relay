# GNOME Shortcut

1. Open GNOME Settings.
2. Open Keyboard Shortcuts.
3. Create a custom shortcut.
4. Enter this command:

```bash
/home/<user>/.local/bin/quiz-relay solve --source shortcut
```

5. Assign `Alt+F9`.
6. Run a test execution.
